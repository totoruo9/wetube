doctype html
html(lang="ko")
    head
        title #{pageTitle} | #{siteName} 
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        header 
            h1=pageTitle
            nav
                ul 
                    li
                        a(href="/") Home
                    if !loginState
                        li 
                            a(href="/login") Login
                        li
                            a(href="/join") Join
                    else 
                        li 
                            a(href="/users/logout") Logout
                        li 
                            a(href="/user-profile") #{user.name}의 Profile
                    li
                        a(href="/search") Search
                    li
                        a(href="/videos/upload") Upload Video
        main
            block content
    include partials/footer.pug